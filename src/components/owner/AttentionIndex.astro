---
interface Props { number: '01' | '02'; label: string }
const { number, label } = Astro.props;
---
<div class="attention-index" aria-label={label}>
  <span aria-hidden="true">{number}</span><span class="attention-label">{label}</span>
</div>
